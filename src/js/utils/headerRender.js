import MainApi from '../api/MainApi';
import { apiConfig } from '../components/baseComponents';
import { logoutButton, regButton } from '../constants/constants';

const mainApi = new MainApi(apiConfig);
