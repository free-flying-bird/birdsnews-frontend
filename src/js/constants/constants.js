const successButton = document.querySelector('.popup__alt-button_type_success');
const errorMessage = document.querySelector('.popup__title_type_error');
const searchInput = document.querySelector('.search__input');
const searchButton = document.querySelector('.search__button');
const articles = document.querySelector('.articles');
const articlesContainer = document.querySelector('.articles__container');
const regButton = document.querySelector('.header__button_type_reg');
const regAltButton = document.querySelector('.popup__alt-button_type_reg');
const loginAltButton = document.querySelector('.popup__alt-button_type_login');
const wrapMenu = document.querySelector('.header__menu-wrap');
const logoutButton = document.querySelector('.logout-button');
const logoutIcon = document.querySelector('.button__icon');
const navigation = document.querySelector('.header__navigation');
const header = document.querySelector('.header');
const headerTitle = document.querySelector('.header__title');
const headerLinks = document.querySelectorAll('.header__link');
const headerButton = document.querySelector('.header__button');
const articlesTitle = document.querySelector('.articles__title');
const articlesTags = document.querySelector('.articles__tags');

export {
  searchInput,
  errorMessage,
  successButton,
  searchButton,
  articles,
  articlesContainer,
  regButton,
  regAltButton,
  loginAltButton,
  wrapMenu,
  logoutButton,
  logoutIcon,
  navigation,
  header,
  headerTitle,
  headerLinks,
  headerButton,
  articlesTitle,
  articlesTags
}